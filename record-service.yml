server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scdfinal
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: NewStrongPassword123!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
